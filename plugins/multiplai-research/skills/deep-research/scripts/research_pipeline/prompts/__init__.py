"""Focused prompt templates for each LLM node.

Each node has a dedicated module so prompts are versioned alongside the code
that uses them. Templates are Python string constants with .format() fields.
"""
