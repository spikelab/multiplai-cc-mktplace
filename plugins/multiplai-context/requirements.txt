# Runtime dependencies are no longer pinned here.
#
# scripts/pyproject.toml declares them once, as a member of the repo-root uv
# workspace, resolved from the committed uv.lock. The shared core (anthropic,
# claude-agent-sdk, pyyaml) arrives transitively through `multiplai-core`.
# The test/dev toolchain (pytest et al) is supplied per-run by CI via
# `uv run --with pytest ...` — see .github/workflows/ci.yml.
