# Test/dev toolchain for the multiplai-context plugin.
# The test suite imports multiplai_core directly (the scripts get it via their
# PEP 723 inline metadata at runtime, but pytest needs it installed here).
multiplai-core @ git+https://github.com/spikelab/multiplai-core@v0.8.1
pytest>=7.0
pytest-asyncio>=0.21
# Docstrings and reviewer commands assume `pytest --timeout` works; without
# this plugin the flag is rejected as unknown.
pytest-timeout>=2.1
