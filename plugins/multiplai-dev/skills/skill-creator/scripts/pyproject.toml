# Dependencies for the skill-creator promotion/scaffolding scripts.
[project]
name = "skill-creator-scripts"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["pyyaml>=6.0"]

[tool.uv]
package = false
