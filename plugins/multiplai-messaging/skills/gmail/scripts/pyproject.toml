# Dependencies for the gmail skill.
[project]
name = "gmail-scripts"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "multiplai-core",
    "google-api-python-client>=2.198.0,<3",
    "google-auth-oauthlib>=1.4.0,<2",
    "google-auth[requests]>=2.56.2,<3",
]

[tool.uv]
package = false
