# Dependencies for the slack skill.
[project]
name = "slack-scripts"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "multiplai-core",
    "slack_sdk>=3.27,<4",
]

[tool.uv]
package = false
